#!/usr/bin/env node
import { syncSkills } from "./lib/skill-supply-chain.mjs";

try {
  process.stdout.write(`${syncSkills({ check: process.argv.includes("--check") })}\n`);
} catch (error) {
  process.stderr.write(`${error.message}\n`);
  process.exitCode = 1;
}
