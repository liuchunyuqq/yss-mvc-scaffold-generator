#!/usr/bin/env node
import { exportSkills } from "./lib/export-skills.mjs";
try { process.stdout.write(`${exportSkills()}\n`); } catch (error) { process.stderr.write(`${error.message}\n`); process.exitCode = 1; }
