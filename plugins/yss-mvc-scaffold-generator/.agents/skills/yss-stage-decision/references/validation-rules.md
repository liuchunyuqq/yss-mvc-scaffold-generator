# 验证规则

验证分为结构、引用和语义三层：

1. **结构**：Schema 版本、稳定 ID、状态枚举、必填字段、条件字段和证据格式。
2. **引用**：上下文端点、场景、不变量、术语和下游资产引用必须可解析。
3. **语义**：禁止关系自环、未知上下文、未批准 Shared Kernel、未解释的语义/技术方向差异、职责重叠、无责任人的不变量和跨上下文直接共享实体。

以下情况必须 `blocked`：关键规则所有权未知、关键术语冲突、上下文边界未定、关键失败场景缺失、事件语义无法解释、领域专家确认缺失。

以下变化应触发精确传播：

- 术语含义变化 → Spec、验收、原型或 API；
- 上下文边界变化 → 系统架构、OpenAPI、数据和切片；
- Context Map 关系变化 → 集成契约和模型翻译；
- 不变量变化 → 测试 seam、实现合同和验证策略。

验证器允许“语义方向 ≠ 技术方向”，但要求 `direction_explanation` 和翻译责任非空。
