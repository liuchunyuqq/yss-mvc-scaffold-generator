# 数据分析研发流程

```text
需求与数据契约确认
        ↓
规格冻结（需求 + MD + API/结果契约）
        ↓
垂直切片开发
        ↓
自动门禁：编译、单测、SQL只读校验、MD一致性、集成测试
        ↓
一次人工发布确认
        ↓
发布与运行监控
```

取消独立的需求评审、架构评审、测试评审、发布评审。权限、数据源、DDL、跨库和性能阈值变化仍需专项审查与回滚方案。
