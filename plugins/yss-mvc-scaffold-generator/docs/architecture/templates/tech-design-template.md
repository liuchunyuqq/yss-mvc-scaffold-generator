# 技术方案：[功能名称]

> 版本: 0.1 | 作者: | 日期: | 关联: Spec-xxx / ADR-xxx

---

## 背景

（为什么需要这个方案？解决什么问题？有何约束？）

---

## 方案概述

（2-3 句话描述核心思路）

---

## 详细设计

### 业务架构影响

（本方案影响哪些用户旅程、价值流、角色职责、外部系统边界；如无影响，说明原因。）

相关图：`docs/.scratch/<feature>/architecture/diagrams/<feature>-business-capability.excalidraw`

### 功能架构

（功能域、模块边界、优先级、模块依赖、MVP / 非目标范围；说明哪些内容需要回填 Spec。）

相关图：`docs/.scratch/<feature>/architecture/diagrams/<feature>-functional-modules.excalidraw`

### 系统总体架构

（服务/模块边界、部署形态、集成关系、权限链路、性能/可靠性/可观测性、发布和回滚策略。）

相关图：`docs/.scratch/<feature>/architecture/diagrams/<feature>-system-architecture.excalidraw`

### 已明确的认证、授权、租户隔离、敏感数据或合规行为（条件适用）

仅当 Spec、冻结契约或既有架构约束明确改变认证、授权、租户隔离、敏感数据或合规行为时，说明责任边界、数据/API 影响、审计或加密要求、验证 seam 与回滚策略。沿用既有中间件、未变化的 `401` / `403`、普通字段、SQL / DDL / 迁移或上传 / 下载本身不填写本节，也不新增独立安全审查。

### 数据模型

```sql
-- 示例：DDL 或 ER 图描述
CREATE TABLE xxx (
    id UUID PRIMARY KEY,
    ...
);
```

（如果是数据模型、元数据、版本或血缘分析类产品，必须补充概念模型、逻辑模型、物理模型、元模型、版本策略、血缘/查询/索引策略。）

相关图：`docs/.scratch/<feature>/architecture/diagrams/<feature>-data-model.excalidraw`

### API 设计

| Method | Path | 说明 | Request | Response |
|--------|------|------|---------|----------|
| GET | /api/v1/xxx | | | |
| POST | /api/v1/xxx | | | |

### 核心流程

（时序图或流程图描述关键交互）

### 关键算法

（如有复杂计算逻辑，描述伪代码）

---

## 方案对比

| 维度 | 方案A: [名称] | 方案B: [名称] | 方案C: [名称] ★推荐 |
|------|-------------|-------------|-------------------|
| 复杂度 (1-5) | | | |
| 安全 (1-5) | | | |
| 性能 (1-5) | | | |
| 扩展性 (1-5) | | | |
| 开发成本 (人天) | | | |
| 与现有技术栈契合度 | | | |

### 选择理由

（为什么推荐方案C）

---

## 风险评估

| 风险 | 概率 (高/中/低) | 影响 | 缓解措施 | Owner |
|------|---------------|------|---------|-------|
| | | | | |

---

## 性能评估

| 指标 | 目标 | 方案 |
|------|------|------|
| 预期 QPS | | |
| P99 响应时间 | ≤ 200ms | |
| 数据量 (1年) | | |
| 缓存策略 | | Redis / CDN / 浏览器缓存 |
| 数据库索引 | | |

---

## 技术债务

| 妥协点 | 原因 | 偿还计划 |
|--------|------|---------|
| | | Sprint X |

---

## 变更历史

| 日期 | 版本 | 变更 | 作者 |
|------|------|------|------|
