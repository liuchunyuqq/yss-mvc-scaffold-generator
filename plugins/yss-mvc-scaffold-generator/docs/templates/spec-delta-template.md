# <功能名称> Spec Delta

> 适用场景：修改既有冻结 Spec 基线时，API、状态机、数据模型、跨端协作或其他高风险行为需要在 Spec、OpenAPI Draft、Design Review 和垂直切片之间明确差异。全新产品或全新模块不使用本模板。明确改变认证或授权行为时，把差异作为普通行为写入本 Delta，不新增单独 Delta。
> 本模板只借鉴 OpenSpec 的行为差异表达方式，不引入 OpenSpec CLI、旧式变更目录、变更状态文件或额外状态机。

## 1. 变更摘要

| 项 | 内容 |
|---|---|
| 需求 / Spec |  |
| 相关 OpenAPI Draft / Freeze |  |
| 相关架构 / 设计审查 |  |
| 相关 Ticket / 切片 |  |
| 变更类型 | 新增 / 修改 / 移除 / 高风险修正 |
| 是否阻塞实现 | 是 / 否 |

## 2. 行为差异

| 类型 | 行为 | 当前状态 | 目标状态 | 影响范围 |
|---|---|---|---|---|
| ADDED |  |  |  |  |
| MODIFIED |  |  |  |  |
| REMOVED |  |  |  |  |

说明：

- `ADDED`：新增用户可见行为、API 行为、状态、权限或数据约束。
- `MODIFIED`：已有行为的输入、输出、状态流、权限、错误结构、兼容性或验收标准发生变化。
- `REMOVED`：删除或废弃已有行为、字段、入口、权限、状态或兼容路径。

## 3. 受影响范围

| 维度 | 是否影响 | 说明 / 证据 |
|---|---|---|
| UI / 页面 / 交互状态 | 是 / 否 |  |
| API / OpenAPI schema / 错误结构 | 是 / 否 |  |
| 权限 / 认证 / 授权 | 是 / 否 |  |
| 状态机 / 业务流程 | 是 / 否 |  |
| 数据模型 / Repository / 查询索引 | 是 / 否 |  |
| 外部系统 / 跨端协作 | 是 / 否 |  |
| 发布 / 回滚 / 运维 | 是 / 否 |  |
| 风险 / 人审点 | 是 / 否 |  |

## 4. 验收场景

| 场景 ID | 关联行为 | 前置条件 | 操作 | 期望结果 | 异常 / 边界 |
|---|---|---|---|---|---|
| SD-001 |  |  |  |  |  |
| SD-002 |  |  |  |  |  |

## 5. 测试与 fresh verification 映射

| 场景 ID | 测试层级 | 测试 / 验证命令 | 证据路径 | 状态 |
|---|---|---|---|---|
| SD-001 | 单元 / 契约 / E2E / 手工验证 |  |  | planned / passed / blocked |
| SD-002 | 单元 / 契约 / E2E / 手工验证 |  |  | planned / passed / blocked |

## 6. 未决问题与人审点

| 问题 / 风险 | 负责人 | 阻塞级别 | 处理方式 |
|---|---|---|---|
|  |  | blocking / non-blocking |  |

## 7. 结论

- 是否允许进入 OpenAPI Freeze / no API impact 记录：
- 是否允许进入 `to-tickets`：
- 必须带入垂直切片的约束：
- Git checkpoint 状态：
